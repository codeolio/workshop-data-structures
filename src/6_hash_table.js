const { LinkedList, Node } = require('./4_linked_list.js');

function HashTable (size) {
 // set array up based on size
};


// prototype methods

function hash(key, size) {
  // logic for making hash function
}